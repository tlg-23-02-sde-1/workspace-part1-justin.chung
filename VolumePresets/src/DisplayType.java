/*
 * These are DisplayType objects (NOT strings)
 *  These are the only 5 displaytpe objects possible
 *  Referenced as DispalyType.LED, DisplayType.LCD, etc.
 */

enum DisplayType {
    LED, LCD, OLED, CRT, PLASMA
}